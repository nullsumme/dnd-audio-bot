apiVersion: v1
kind: Service
metadata:
  name: {{ include "dnd-audio-bot.fullname" . }}
  labels:
    {{- include "dnd-audio-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dnd-audio-bot.selectorLabels" . | nindent 4 }}
